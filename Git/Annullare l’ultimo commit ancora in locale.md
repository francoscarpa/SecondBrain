Questa operazione vale quando il *commit* è ancora **in locale**, ossia non è stato *pushato* sul *server*:

```
git reset --soft HEAD~1
```

Ciò rimette le modifiche presenti nel *commit* nello stato di ***unstaged***.